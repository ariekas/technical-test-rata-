# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Customer {
  id: ID!
  name: String!
  email: String!
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type PaginatedCustomer {
  items: [Customer!]!
  total: Int!
  page: Int!
  limit: Int!
  totalPages: Int!
}

type Doctor {
  id: ID!
  name: String!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type PaginatedDoctor {
  items: [Doctor!]!
  total: Int!
  page: Int!
  limit: Int!
  totalPages: Int!
}

type Schedule {
  id: ID!
  objective: String!
  customerId: String!
  doctorId: String!
  scheduledAt: DateTime!
  createdAt: DateTime!
  updatedAt: DateTime!
  customer: Customer
  doctor: Doctor
}

type PaginatedSchedule {
  items: [Schedule!]!
  total: Int!
  page: Int!
  limit: Int!
  totalPages: Int!
}

type Query {
  hello: String!
  customers(pagination: PaginationInput): PaginatedCustomer!
  customer(id: String!): Customer
  doctor(id: String!): Doctor
  doctors(pagination: PaginationInput): PaginatedDoctor!
  schedules(pagination: PaginationInput, filter: ScheduleFilterInput): PaginatedSchedule!
  schedule(id: String!): Schedule
}

input PaginationInput {
  page: Int! = 1
  limit: Int! = 10
}

input ScheduleFilterInput {
  customerId: String
  doctorId: String
  date: DateTime
}

type Mutation {
  createCustomer(input: CreateCustomerInput!): Customer!
  updateCustomer(input: UpdateCustomerInput!): Customer!
  deleteCustomer(id: String!): Customer!
  createDoctor(input: CreateDoctorInput!): Doctor!
  updateDoctor(input: UpdateDoctorInput!): Doctor!
  deleteDoctor(id: String!): Doctor!
  createSchedule(input: CreateScheduleInput!): Schedule!
}

input CreateCustomerInput {
  name: String!
  email: String!
}

input UpdateCustomerInput {
  name: String
  email: String
  id: ID!
}

input CreateDoctorInput {
  name: String!
}

input UpdateDoctorInput {
  name: String
  id: ID!
}

input CreateScheduleInput {
  objective: String!
  customerId: String!
  doctorId: String!
  scheduledAt: DateTime!
}